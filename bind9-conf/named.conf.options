;; Only the forwarders needs to be changed in this file, other options are predefinied
options {
        directory "/var/cache/bind";

        forwarders {
                192.168.178.1;
        };

        dnssec-validation auto;

        listen-on-v6 { any; };
};
